#pragma once
// Template for secrets.h. Copy this file to secrets.h and set your own values:
//
//   cp secrets.h.example secrets.h
//
// secrets.h is gitignored so credentials never land in version control.

#define OTA_PASSWORD "changeme"
